spring.application.name=TFG Backend

# JPA Configuration for Tests - STRICT MODE
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.default_schema=public

# Enable foreign key constraints validation
spring.jpa.properties.hibernate.check_nullability=true

# Fail fast on constraint violations
spring.jpa.properties.hibernate.jdbc.batch_versioned_data=true

spring.jpa.open-in-view=false

# Ensure Flyway is enabled for schema creation
spring.flyway.enabled=true

# JWT Configuration for Tests
# Using the same secret as production for simplicity in development/testing
# In real production environments, different secrets should be used
jwt.secret=4d635166546a576e5a7234753778214125442a472d4b6150645367566b597033
jwt.expiration=86400000