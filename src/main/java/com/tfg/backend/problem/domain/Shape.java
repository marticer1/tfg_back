package com.tfg.backend.problem.domain;

/**
 * Enum representing the possible shapes for nodes in problem visualization.
 */
public enum Shape {
    SQUARE,
    ROUND,
    STAR,
    TRIANGLE
}
